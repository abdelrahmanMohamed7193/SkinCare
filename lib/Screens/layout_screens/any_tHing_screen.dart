import 'package:flutter/material.dart';

class AnyTHingScreen extends StatelessWidget {
  const AnyTHingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Any Thing')),
    );
  }
}
